<Window x:Class="IDPersonalSecure.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historial de compartidos" Height="520" Width="620"
        WindowStartupLocation="CenterOwner"
        Icon="pack://application:,,,/Assets/app.ico"
        Background="{StaticResource BgBrush}"
        TextElement.Foreground="{StaticResource TextBrush}">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Historial de documentos compartidos"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
        <TextBlock x:Name="EmptyText" DockPanel.Dock="Top" Text="Aún no has compartido ningún documento."
                   Foreground="{StaticResource MutedBrush}" Visibility="Collapsed"/>
        <ListView x:Name="LogList" Background="Transparent" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Margin" Value="0,4"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Line1}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Line2}" Foreground="{StaticResource MutedBrush}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding RecipientDisplay}" Foreground="{StaticResource AccentBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Editar destinatario" Style="{StaticResource GhostButton}"
                                    VerticalAlignment="Center" Click="EditRecipient_Click"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</Window>
