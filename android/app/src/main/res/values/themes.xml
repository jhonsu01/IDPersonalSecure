<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base del sistema (la UI real la dibuja Jetpack Compose / Material 3). -->
    <style name="Theme.IDPersonalSecure" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
